How to deploy the schema
------------------------

1) Copy the file ${project.artifactId}-${project.version}.jar to the GeoNetwork WEB-INF/lib folder.

2) Copy the folder iso19115-3.2018-deeca to the schemas folder.

A restart of GeoNetwork is required.
